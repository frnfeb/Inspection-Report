Icons in this project are loaded from the Lucide icon set via CDN
(`<svg data-lucide="...">` + https://unpkg.com/lucide) — see js/app.js `initIcons()`.

Drop custom/brand-specific SVG icons in this folder if you need icons
outside the Lucide set, then reference them with a plain <img> or inline <svg>.
